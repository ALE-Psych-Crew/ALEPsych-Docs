# Mod Not Detected

## Checks

1. Folder is exactly `mods/<name>`.
2. Folder name is not in Mods-menu ignore list.
3. `CONTENT_MOD` path (if used) points to a valid mod name.
4. Saved `currentMod` is valid.
