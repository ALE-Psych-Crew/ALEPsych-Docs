# Platform-Specific Assets

::: callout warning "Unfinished (WIP)"
This page is still being worked on and may change.
:::

ALE-Psych has platform-specific runtime branches (desktop/mobile), but most mod content still relies on the same logical asset paths.

Use one canonical folder layout, then verify runtime behavior on each target platform.
