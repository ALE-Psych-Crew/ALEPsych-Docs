# Mod no detectado

## Cheques

1. La carpeta es exactamente `mods/<name>`.
2. El nombre de la carpeta no está en la lista de ignorados del menú Mods.
3. La ruta `CONTENT_MOD` (si se usa) apunta a un nombre de mod válido.
4. El `currentMod` guardado es válido.